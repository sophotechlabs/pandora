apiVersion: v1
kind: Service
metadata:
  name: {{ include "pandora.fullname" . }}
  labels:
    {{- include "pandora.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "pandora.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: web
